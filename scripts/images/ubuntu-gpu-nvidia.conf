IMAGE_NAME="ubuntu-nvidia-gpu"
IMAGE_DESCRIPTION="Ubuntu 26.04 NVIDIA GPU guest image — NVIDIA server driver (DKMS pre-built), Python toolchain"
DISTRO="ubuntu"
UBUNTU_VERSION="26.04"
UBUNTU_CODENAME="resolute"

# 16G provides headroom for kernel headers, NVIDIA driver, DKMS build artefacts,
# Docker CE, and nvidia-container-toolkit.
IMAGE_SIZE="16G"

SETUP_SCRIPT="scripts/images/ubuntu-gpu-nvidia-setup.sh"

AMI_NAME="ubuntu-26.04-nvidia-gpu-x86_64"

SYSTEM_TAG="gpu-vendor=nvidia"
