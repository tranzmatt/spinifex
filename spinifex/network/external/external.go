// Package external is L5 of the spinifex network stack: VPC-facing external
// connectivity (IGW, EIP, NAT Gateway). Uses L1 (ovn), L3 (policy), and L0
// (host) uplink CIDR resolved at startup.
package external
