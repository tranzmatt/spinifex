# Load the NVIDIA modules at boot so /dev/nvidia* exist before CDI generation
# and the container runtime start. A headless node never runs an X server or a
# CUDA app, so nothing triggers the on-demand load — without this the device
# nodes are simply absent and CDI generation finds no GPU.
nvidia
nvidia_uvm
nvidia_modeset
