# nvidia.ko and nouveau conflict — without this blacklist the NVIDIA driver
# will not bind after boot. This is the guest-side blacklist only, distinct
# from the host-side blacklist `spx admin gpu setup` configures for VFIO
# passthrough.
blacklist nouveau
options nouveau modeset=0
