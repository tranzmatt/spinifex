# Add the cloud-init default user (UID 1000) to the docker group at first
# boot, so the interactive user can drive Docker without sudo. The user does
# not exist at build time — cloud-init creates it — so this cannot be a
# build-time usermod.
runcmd:
  - usermod -aG docker $(id -un 1000) || true
